import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        height: '80%',
        width: '100%',
        alignSelf: 'center'
    },
    closeIcon: {
        position: 'absolute',
        right: 20,
        zIndex: 9999,
        top: '5%'
    }
});

export default styles